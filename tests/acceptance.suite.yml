actor: AcceptanceTester
modules:
    enabled:
        #- PhpBrowser:
        - \Helper\AizelDevWebDriver:
            url: '%TEST_HOST%'
            branch: '%TEST_BRANCH%'
            window_size: 1920x1080
            browser: chrome
            port: 9515
            window_size: 'false'
            capabilities:
                goog:chromeOptions:
                    args: ["--headless", "--disable-gpu"]

        - \Helper\Acceptance
